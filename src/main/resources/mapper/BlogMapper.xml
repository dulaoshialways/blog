<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="club.dulaoshi.blog.dao.BlogDao">

	<resultMap type="Blog" id="BlogResult">
		<result property="id" column="id"/>
		<result property="title" column="title"/>
		<result property="summary" column="summary"/>
		<result property="releaseDate" column="releaseDate"/>
		<result property="clickHit" column="clickHit"/>
		<result property="replyHit" column="replyHit"/>
		<result property="content" column="content"/>
		<result property="keyWord" column="keyWord"/>
		
<!--		<association property="blogType" column="typeId" select="club.dulaoshi.blog.dao.BlogTypeDao.findById"></association>-->
	</resultMap>
	
	
	<select id="countList" resultMap="BlogResult">
		SELECT
			DATE_FORMAT(releaseDate, '%Y年%m月')AS releaseDateStr,
			count(*)AS blogCount
		FROM
			t_blog
		GROUP BY
			DATE_FORMAT(releaseDate, '%Y年%m月')
		ORDER BY
			DATE_FORMAT(releaseDate, '%Y年%m月')DESC
	</select>

	<select id="list" parameterType="Map" resultMap="BlogResult">
		select
			tb.id,
			tb.title,
			tb.summary,
			tb.releaseDate,
			tb.clickHit,
			tb.replyHit,
			tb.content,
			tb.mdContent,
			tb.htmlContent,
			tbt.typeName,
			tb.keyWord,
            tb.state,
            tb.flag,
			tb.authorName
		from t_blog tb left join t_blogtype tbt on tb.typeId = tbt.id
		<where>
			flag = 0
			<if test="state != null">
				and tb.state = #{state}
			</if>
			<if test="title != null and title != ''">
				and tb.title like #{title}
			</if>
			<if test="typeId != null and typeId != ''">
				and tbt.id = #{typeId}
			</if>
			<if test="releaseDateStr != null and releaseDateStr != ''">
				and DATE_FORMAT(tb.releaseDate, '%Y年%m月') = #{releaseDateStr}
			</if>
		</where>
		order by tb.id desc
		<if test="start != null and size != null">
			limit #{start},#{size}
		</if>
	</select>
	
	<select id="getTotal" parameterType="Map" resultType="Long">
		select count(*) from t_blog
		<where>
			flag = 0
			<if test="state != null">
				and state = #{state}
			</if>
			<if test="title != null and title != ''">
				and title like #{title}
			</if>
			<if test="typeId != null and typeId != ''">
				and typeId = #{typeId}
			</if>
			<if test="releaseDateStr != null and releaseDateStr != ''">
				and DATE_FORMAT(releaseDate, '%Y年%m月') = #{releaseDateStr}
			</if>
		</where>
	</select>
	
	<select id="findById" parameterType="Integer" resultMap="BlogResult">
		select
			tb.id,
			tb.title,
			tb.summary,
			tb.releaseDate,
			tb.clickHit,
			tb.replyHit,
			tb.content,
			tb.mdContent,
			tb.htmlContent,
			tbt.typeName,
			tb.keyWord,
			tb.typeId,
			tb.state,
			tb.flag,
			tb.authorName
		from t_blog
			tb left join t_blogtype tbt on tb.typeId = tbt.id
		where tb.id=#{id}
	</select>
	
	<update id="update" parameterType="Blog">
		update t_blog
		<set>
			<if test="title != null and title!=''">
				title = #{title},
			</if>
			<if test="content != null and content !=''">
				content = #{content},
			</if>
			<if test="mdContent != null and mdContent != ''">
				mdContent = #{mdContent},
			</if>
			<if test="htmlContent != null and htmlContent != ''">
				htmlContent = #{htmlContent},
			</if>
			<if test="typeId != null">
				typeId = #{typeId},
			</if>
			<if test="keyWord != null and keyWord != ''">
				keyWord = #{keyWord},
			</if>
			<if test="clickHit != null">
				clickHit = #{clickHit},
			</if>
			<if test="replyHit != null">
				replyHit = #{replyHit},
			</if>
			<if test="summary!=null and summary!=''">
				summary=#{summary},
			</if>
			<if test="state != null">
				state = #{state},
			</if>
		</set>
		where id = #{id}
	</update>
	
	<select id="getLastBlog" parameterType="Integer" resultMap="BlogResult">
		select id,title from t_blog where id &gt; #{id} order by id asc limit 1;
	</select>
	
	<select id="getNextBlog" parameterType="Integer" resultMap="BlogResult">
		select id,title from t_blog where id &lt; #{id} order by id desc limit 1;
	</select>
	
	<insert id="add" useGeneratedKeys="true" keyProperty="id" parameterType="Blog">
		insert into t_blog values(null,#{title},#{summary},now(),0,0,#{content},#{mdContent},#{htmlContent} ,#{typeId},#{keyWord},0,#{state},#{authorName})
	</insert>
	
	<update id="deleteById" parameterType="Integer">
		update t_blog tb set tb.flag = 1 where tb.id =#{id}
	</update>

	<update id="deleteByState" parameterType="Integer">
		update t_blog tb set tb.state = 1 where tb.id =#{id}
	</update>
	
	
	<select id="getBlogByTypeId" parameterType="Integer" resultType="Integer">
		select count(*) from t_blog tb where tb.typeId = #{typeId} and  tb.flag = 0
	</select>
	
</mapper> 








